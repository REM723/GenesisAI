# Dev tooling shared across the Python workspace. Install: pip install -r requirements-dev.txt
ruff>=0.6.9
mypy>=1.11
pytest>=8.3
httpx>=0.27          # FastAPI TestClient transport
pydantic>=2.8        # for mypy pydantic plugin
pytest-asyncio>=0.24 # async integration tests
