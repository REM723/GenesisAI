[project]
name = "genesisai-agents"
version = "0.1.0"
description = "GenesisAI agents: prompt optimizer + loop engine (Phase 4), LangGraph workflow (Phase 5)"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["genesis_agents"]
