# packages/shared

Shared TypeScript types and utilities for the web client. Types are generated from the
backend schema (Phase 1+) rather than hand-duplicated.

**Status:** stub — no exports yet.
**Public interface:** `@genesisai/shared` (barrel export in `index.ts`).
**Tests:** `pnpm --filter @genesisai/shared exec tsc --noEmit`.
